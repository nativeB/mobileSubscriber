import { ServiceTypes } from "../types/subscriber";


const SERVICE_TYPES: ServiceTypes[]  = [ "MOBILE_PREPAID", "MOBILE_POSTPAID"]

const constants = Object.freeze({
    SERVICE_TYPES
})

export default constants;